# Gastown Orchestration: Bead 0002

## Goal
Implement Strategic Intelligence (Inference & Authority) using specialized agent roles.

## Assignment Table

| Role | Agent Assignment | Focus Area |
| :--- | :--- | :--- |
| **Spec Agent** | `generalist` | Update `03-bmad-spec.md` with Inference and Authority requirements. |
| **Domain Agent** | `codebase_investigator` | Design `AuthorityService` and `InferenceService` interfaces. |
| **Graph Agent** | `generalist` | Implement JCR-backed reasoning logic and SKOS mapping. |
| **AEM Adapter Agent** | `generalist` | Build Granite UI Side-panel components for the "Semantic Rail". |
| **Test Agent** | `generalist` | Verify Confidence-score logic and Inference unit tests. |

## Execution Sequence
1. **Spec & Domain Phase**: (Turns 1-3) Define the rules for "Self-Healing" data.
2. **Logic Phase**: (Turns 4-8) Implement the `core` services.
3. **UI Phase**: (Turns 9-12) Expose the graph in the AEM Assets console.
4. **Validation**: (Turns 13+) Final "Green" build and GitHub push.
