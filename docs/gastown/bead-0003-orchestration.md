# Gastown Orchestration: Bead 0003

## Goal
Implement Visual Magic (AI Vision, UI Rails, SEO) using Gastown Agent roles.

## Assignment Table

| Role | Agent Assignment | Focus Area |
| :--- | :--- | :--- |
| **Spec Agent** | `generalist` | Define the visual "Semantic Rail" requirements and SEO schema mapping. |
| **Domain Agent** | `codebase_investigator` | Design the `SemanticVision` aggregate and `SeoProvider` interfaces. |
| **AEM Adapter Agent** | `generalist` | Implement the Workflow Step for AI Vision and the Granite UI side-panel. |
| **Graph Agent** | `generalist` | Map visual features to Schema.org triples. |
| **Test Agent** | `generalist` | Verify JSON-LD generation and workflow execution. |

## Execution Sequence
1. **Vision Phase**: (Turns 1-4) Implement the `SemanticVisionWorkflowProcess` to "look" at assets.
2. **SEO Phase**: (Turns 5-8) Build the JSON-LD injection service for AEM pages.
3. **UI Phase**: (Turns 9-12) Create the "Semantic Rail" side-panel in `ui.apps`.
4. **Final Integration**: (Turns 13+) "Masterpiece" validation and push.
