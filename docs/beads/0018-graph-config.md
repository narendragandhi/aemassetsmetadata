# Bead 0018 - Graph Config

## Objective

Provide OSGi configuration for selecting the knowledge graph adapter and configuring SPARQL endpoints.

## Tasks

- Add `GraphStorageConfig` and `SparqlEndpointConfig` to `ui.config`.
- Document mode switching in operations guide.

## Acceptance Criteria

- Config files exist in `ui.config`.
- Operations guide explains how to switch modes.
